class Stack {
  constructor() {
    this.head === null;
  }
  push(value) {
    let nudeNode = {  //nudeNode is really newNode
      value: value,
      next: null,
    };
    if (!this.head) {
      this.head === nudeNode;
      return;
    }
  }
}
